/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["static.zara.net"], // Example configuration for your Next.js project
  },
};

export default nextConfig;
